---
title: erouter
---

###  Introduction
Router module

###  Usage
```go
import (
	"github.com/wallace5303/ee-go/erouter"
)
```

###  API List
Brief introduction only. Following Go conventions, detailed information for constants/functions/methods is in the source code.

###  func Handle(httpMethod, path string, handler HandlerFunc)
Register a route

###  type HandlerFunc

###  type Ctx
```
type Ctx struct {
	GinCtx *gin.Context   // gin context
	Err    any            // Request error
	Timed  int64          // Execution time
	Args   map[string]any // Request parameters
}
```

### func (c *Ctx) JSON(data any)
Return data in JSON format

###  func (c *Ctx) JSONWithCode(code int, data any)
Return data in JSON format, including HTTP status code

### ArgJson() (arg map[string]any, ok bool)
Parse JSON format parameters